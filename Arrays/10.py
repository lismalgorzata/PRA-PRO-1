def suma(array):
    suma=0
    for i in array:
        suma=suma+i
    return suma
        
array=[4,3,7,1,3]
x=suma(array)
print(f"Array: {array}, sum of values: {x}")
#####################
def sum(array):
    suma=0
    for i in range(0, len(array)):
        strarray+=str(f"{array[i]}")
    return strarray
def array2str(array):
    strarray=""
    for i in range(0,len(array)):
        strarray+=str(f"{array[i]}")
    return strarray

    
